// Modules: Organize your code by separating the Rectangle class and the Person interface into separate
// modules. Demonstrate how to import and use them in the main program file.

export interface Person {
    name: string;
    age: number;
    email: string;
}